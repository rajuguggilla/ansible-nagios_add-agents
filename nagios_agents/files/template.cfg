###############################################################################
# LOCALHOST.CFG - SAMPLE OBJECT CONFIG FILE FOR MONITORING THIS MACHINE
#
#
# NOTE: This config file is intended to serve as an *extremely* simple
#       example of how you can create configuration entries to monitor
#       the local (Linux) machine.
#
###############################################################################
#
# HOST DEFINITION
#
###############################################################################

# Define a host for the local machine

define host{
        use                     linux-server            ; Name of host template to use
                                                        ; This host definition will inherit all variables that are defined
                                                        ; in (or inherited by) the linux-server host template definition.
        host_name               XXXX
        alias                   XXXX
        address                 ZZZZ
        }


###############################################################################
#
# HOST GROUP DEFINITION
#
###############################################################################

# Define an optional hostgroup for Linux machines

#define hostgroup{
#        hostgroup_name  XXXX ; The name of the hostgroup
#        alias            	; Long name of the group
#        members         XXXX     ; Comma separated list of hosts that belong to this group
#        }



###############################################################################
#
# SERVICE DEFINITIONS
#
###############################################################################


# Define a service to "ping" the local machine

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             PING
        check_command                   check_ping!100.0,20%!500.0,60%
        }


# Define a service to check the disk space of the root partition
# on the local machine.  Warning if < 20% free, critical if
# < 10% free space on partition.

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             Disk Usage 
        check_command                   check_nrpe_3arg!check_disk!20%!10%!/
        }



# Define a service to check the number of currently logged in
# users on the local machine.  Warning if > 20 users, critical
# if > 50 users.

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             Current Users
        check_command                   check_nrpe_2arg!check_users!20!50
        }


# Define a service to check the number of currently running procs
# on the local machine.  Warning if > 250 processes, critical if
# > 400 processes.

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             Total Processes
        check_command                   check_nrpe_2arg!check_total_procs!250!400
        }



# Define a service to check the load on the local machine.

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             Current Load
        check_command                   check_nrpe_2arg!check_load!5.0,4.0,3.0!10.0,6.0,4.0 
        }




# Define a service to check SSH on the local machine.
# Disable notifications for this service by default, as not all users may have SSH enabled.

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             SSH
        check_command                   check_ssh
        }

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX 
        service_description             CPU Usage
        check_command                   check_nrpe_8arg!check_cpu!70!90!70!90!70!90!70!90
        }

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             Memory Usage
        check_command                   check_nrpe_4arg!check_memory!80!90!80!90
        }

define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX
        service_description             Number of Open Files
        check_command                   check_nrpe_2arg!check_open-files!80%!90%
        }


define service{
        use                             local-service         ; Name of service template to use
        host_name                       XXXX 
        service_description             Docker Service
        check_command                   check_nrpe_1arg!check_service!docker
        }

